// Loads 

// Loads user information and populates the table once the window loads
window.onload = function() {
    loadUserInfo(document.querySelector("table"));
}

// Gets user information from the server and loads it into the table defined in the html
function loadUserInfo(table) {

}